def divisors(n):
    sum = 0
    if n % 2 != 0:
        return sum
    else:
        a = (math.sqrt(n))
        i = 1
        while ( i * i < n ):
            if n % i == 0 and i % 2 == 0:
                sum += 1
            if n % i == 0 and (n/i) % 2 == 0:
                sum += 1
            i += 1
        if i * i == n:
            sum += 1
        return (sum)
